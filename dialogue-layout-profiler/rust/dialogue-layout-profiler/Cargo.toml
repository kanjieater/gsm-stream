[package]
name = "dialogue-layout-profiler"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust core for dialogue OCR layout profiling."

[lib]
name = "dialogue_layout_profiler_core"
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
